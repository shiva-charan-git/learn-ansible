- name: Demo Play 1
  hosts: localhost
  vars:
    URL: play.exmmple.com 
  tasks:
    - name: print Hello
      ansible.builtin.debug:
       msg: URL = {{ URL }}



- name: playvariable from files 
  hosts: localhost
  vars_file:
    - var.yml
  tasks:
    - name: print play variable from file
      ansible.builtin.debug:
       msg: URL = {{ URL }}
